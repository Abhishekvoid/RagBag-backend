# StudyWise / RAG-tutor — Django backend runtime dependencies
#
# Deploy target: Render (Linux, Python 3.13).
# Embeddings + reranking run as SEPARATE TEI services — this image ships NO
# torch / transformers / sentence-transformers. See requirements_ml.txt (local
# only) and docker-compose.yml. Configure TEI_URL and RERANK_URL as env vars.
#
# Versions pinned from the working .venv (2026-07-23).

# ---- Django core ----------------------------------------------------------
Django==5.2.4
asgiref==3.9.1
sqlparse==0.5.3
tzdata==2025.2
python-dotenv==1.1.1

# ---- API layer ------------------------------------------------------------
djangorestframework==3.16.0
djangorestframework-simplejwt==5.5.1
djoser==2.3.3
dj-rest-auth==7.0.1
django-allauth==65.10.0
django-cors-headers==4.7.0
PyJWT==2.10.1
cryptography==45.0.5

# ---- Realtime (channels / websockets) -------------------------------------
channels==4.2.2
channels-redis==4.2.1
redis==5.2.1

# ---- Database -------------------------------------------------------------
psycopg2-binary==2.9.10

# ---- Static + object storage ----------------------------------------------
whitenoise==6.9.0
django-storages==1.14.6
boto3==1.40.8

# ---- ASGI server ----------------------------------------------------------
# Start command: gunicorn core.asgi:application -k uvicorn.workers.UvicornWorker
gunicorn==23.0.0
uvicorn[standard]==0.35.0

# ---- Background jobs ------------------------------------------------------
celery==5.5.3
gevent==25.5.1

# ---- LLM / RAG ------------------------------------------------------------
openai==1.98.0
pinecone==9.1.0
tiktoken==0.9.0
httpx==0.28.1
tenacity==9.1.4
numpy==2.2.6

# ---- Document ingestion ---------------------------------------------------
# NOTE: pdf2image needs `poppler-utils` and pytesseract needs `tesseract-ocr`
# installed at the OS level. Render's native Python runtime cannot apt-install
# these — deploy this service with a Dockerfile, or drop these two and render
# pages via PyMuPDF instead.
PyPDF2==3.0.1
pymupdf==1.28.0
pdf2image==1.17.0
pytesseract==0.3.13
python-docx==1.2.0
python-pptx==1.0.2
pillow==11.3.0
lxml==6.0.0
